info:
  name: sasss
  type: http
  seq: 9

http:
  method: POST
  url: "{{BASE_URL}}/api/conversations/1/mark-read/"
  headers:
    - name: Authorization
      value: Bearer {{ACCESS_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
