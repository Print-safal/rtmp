info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/register/"
  body:
    type: json
    data: |-
      {
          "username": "safal2",
          "email": "safal2@test.com",
          "password": "password123",
          "display_name": "Safal 2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
