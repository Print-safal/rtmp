info:
  name: LIST MSG
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/api/messages/?conversation=1"
  params:
    - name: conversation
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
