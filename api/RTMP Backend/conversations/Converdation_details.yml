info:
  name: Converdation_details
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/conversations/1/"
  headers:
    - name: Authorization
      value: Bearer {{ACCESS_TOKEN}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
